int	main(int argc, char **argv)
{
	if (argc != 5)
        exit(1);
    // < file1 cmd1 | cmd2 > file2
    // open, close
    // read, write OK
    // malloc, free OK
    // perror, strerror
    // access
    // dup, dup2
    // execve
    // fork
    // pipe
    // unlink
    // wait
    // waitpid
}